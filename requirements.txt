numpy
scipy
laspy[lazrs]
rasterio
pyproj
matplotlib

# only needed for the KML output of tools/plot_displacement.py
simplekml

# PDAL is optional and only needed to read Entwine (ept.json) or other non-LAS
# formats. It cannot be installed with pip on Windows, use conda instead:
#   conda install -c conda-forge python-pdal
